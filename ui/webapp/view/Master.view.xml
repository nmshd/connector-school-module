<mvc:View
    controllerName="eu.enmeshed.connectorui.controller.Master"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
>
    <f:DynamicPage
        id="masterPage"
        stickySubheaderProvider="iconTabBar"
        class="sapUiNoContentPadding"
    >
        <!-- DynamicPage Title -->
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title
                        text="{i18n>appTitle}"
                        level="H2"
                    />
                </f:heading>
            </f:DynamicPageTitle>
        </f:title>
        <f:content>
            <Table
                id="productsTable"
                inset="false"
                items="{
						path: 'studentModel>/students',
						sorter: {
							path: 'Name'
						}
					}"
                class="sapFDynamicPageAlignContent"
                width="auto"
                busyIndicatorDelay="0"
            >
                <headerToolbar>
                    <OverflowToolbar>
                        <ToolbarSpacer />
                        <Button
                            text="Add Students"
                            press=".onOpenAddStudentsDialog"
                        />
                        <Button
                            text="Download all onboarding PDFs"
                            press=".downloadStudents"
                            width="17.5rem"
                        />
                    </OverflowToolbar>
                </headerToolbar>
                <columns>
                    <Column width="12em">
                        <Text text="Title" />
                    </Column>
                    <Column hAlign="End">
                        <Text text="Status" />
                    </Column>
                    <Column>
                    </Column>
                </columns>
                <items>
                    <ColumnListItem type="Inactive">
                        <cells>
                            <ObjectIdentifier
                                title="{studentModel>givenname} {studentModel>surname}"
                                text="{studentModel>id}"
                            />
                            <Text text="{studentModel>status}" />
                            <Button
                                icon="sap-icon://delete"
                                press=".onDeleteStudent"
                            />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </f:content>
    </f:DynamicPage>
</mvc:View>
