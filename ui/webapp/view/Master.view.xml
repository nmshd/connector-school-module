<mvc:View
    controllerName="eu.enmeshed.connectorui.controller.Master"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
>
    <f:DynamicPage
        id="masterPage"
        stickySubheaderProvider="iconTabBar"
        class="sapUiNoContentPadding"
    >
        <!-- DynamicPage Title -->
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title
                        text="{i18n>appTitle}"
                        level="H2"
                    />
                </f:heading>
            </f:DynamicPageTitle>
        </f:title>
        <f:content>
            <Table
                id="productsTable"
                inset="false"
                items="{
						path: 'studentModel>/students',
						sorter: {
							path: 'Name'
						}
					}"
                class="sapFDynamicPageAlignContent"
                width="auto"
                busyIndicatorDelay="0"
            >
                <headerToolbar>
                    <OverflowToolbar>
                        <Title text="Liste aller vorhandenen Schüler in der Ablage" />
                        <ToolbarSpacer />
                        <Button
                            icon="sap-icon://refresh"
                            text="Aktualisieren"
                            tooltip="Aktualisiert die Liste der Schüler"
                            press=".onRefresh"
                        />
                        <Button
                            icon="sap-icon://upload"
                            text="Importieren"
                            tooltip="Importiert eine Schülerliste"
                            press=".onOpenAddStudentsDialog"
                        />
                        <Button
                            icon="sap-icon://excel-attachment"
                            text="Liste als CSV"
                            tooltip="Exportiert die Liste der Schüler als CSV"
                            press=".onDownloadCSV"
                        />
                        <Button
                            text="Onboarding Dokumente"
                            icon="sap-icon://pdf-attachment"
                            tooltip="Erstellt eine PDF Datei mit allen Onboarding Dokumenten der Schüler"
                            press=".downloadStudents"
                        />
                        <Button
                            text="Abmelden"
                            icon="sap-icon://log"
                            press=".logout"
                        />
                    </OverflowToolbar>
                </headerToolbar>
                <columns>
                    <Column>
                        <Text text="Schüler" />
                    </Column>
                    <Column>
                        <Text text="Status" />
                    </Column>
                    <Column>
                        <Text text="Aktionen pro Schüler" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem type="Inactive">
                        <cells>
                            <ObjectIdentifier
                                title="{studentModel>givenname} {studentModel>surname}"
                                text="{studentModel>id}"
                            />
                            <Text text="{path:'studentModel>status',formatter:'.formatStatus'}" />
                            <HBox columnGap="0.25rem">

                                <Button
                                    icon="sap-icon://pdf-attachment"
                                    text="Onboarding Dokument"
                                    press=".onDownloadPdf"
                                    enabled="{= (${studentModel>status} === 'onboarding')}"
                                    tooltip="{= (${studentModel>status} === 'onboarding' ? 'Ein PDF Dokument für das Onboarding des Schülers erzeugen' : 'Dieser Schüler ist aktiv, sie können kein Onboarding Dokument mehr erzeugen')}" />
                                <Button
                                    icon="sap-icon://inspect"
                                    text="Log"
                                    press=".onStudentLog"
                                    enabled="{= (${studentModel>status} !== 'deleted')}"
                                    tooltip="{= (${studentModel>status} === 'deleted' ? 'Ein gelöschter Schüler besitzt keinen Log mehr.' : 'Zur Fehlerbehandlung, den Log des Schülers aufrufen.')}" />
                                <Button
                                    icon="sap-icon://delete"
                                    type="Reject"
                                    text="Schüler löschen"
                                    press=".onDeleteStudent"
                                    tooltip="{= (${studentModel>status} === 'deleted' ? 'Nur noch die Schüler ID ist für diesen Schüler vorhanden, diese kann hiermit gelöscht werden.' : 'Den Schüler und seine Daten aus der Ablage löschen.')}"
                                />
                            </HBox>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </f:content>
    </f:DynamicPage>
</mvc:View>