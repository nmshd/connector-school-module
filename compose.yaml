services:
  connector:
    image: ghcr.io/nmshd/connector:6.17.1
    container_name: connector-with-school-module
    ports:
      - "8099:80"
    environment:
      - CUSTOM_CONFIG_LOCATION=/config.json
      - transportLibrary__baseUrl
      - transportLibrary__platformClientId
      - transportLibrary__platformClientSecret
    volumes:
      - ./config.json:/config.json:ro
      - ./school-module:/usr/app/node_modules/school-module
    depends_on:
      - mongo

  mongo:
    container_name: mongodb
    hostname: mongo
    image: mongo
    volumes:
      - mongo_data:/data/db
    logging:
      driver: none

volumes:
  mongo_data:
